{
  "name": "markdown-previewer",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://steelvoltage.github.io/markdown-previewer",
  "dependencies": {
    "bulma": "^0.7.4",
    "fcctest": "^1.0.2",
    "gh-pages": "^2.0.1",
    "marked": "^0.6.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-fcctest": "^1.3.5",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
